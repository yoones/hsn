- accept / handle new clients using ssh_poll_*
- Add events logging
- Find a way to load a private key for server's credentials without
   asking for the password in command line (because it won't work for
   a deamon)
-
